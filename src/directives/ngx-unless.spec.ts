import { NgxUnless } from './ngx-unless';

describe('NgxUnless', () => {
  it('should create an instance', () => {
    const directive = new NgxUnless();
    expect(directive).toBeTruthy();
  });
});
